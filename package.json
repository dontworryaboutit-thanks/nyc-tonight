{
  "name": "nyc-tonight",
  "version": "1.0.0",
  "description": "Personal NYC event discovery dashboard",
  "private": true,
  "scripts": {
    "build": "node scripts/run.js",
    "scrape": "node scripts/run.js --scrape-only",
    "score": "node scripts/run.js --score-only"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
