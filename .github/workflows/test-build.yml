name: Test Build (branch)

on:
  push:
    branches:
      - 'claude/**'
  workflow_dispatch:
    inputs:
      mode:
        description: 'probe | build | both'
        required: false
        default: 'both'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      # On push events inputs.mode is empty — run both steps
      - name: Probe sources
        if: ${{ inputs.mode == 'probe' || inputs.mode == 'both' || inputs.mode == '' }}
        run: node scripts/dev/probe-sources.js

      - name: Full build
        if: ${{ inputs.mode == 'build' || inputs.mode == 'both' || inputs.mode == '' }}
        run: node scripts/run.js

      - name: Upload build output
        if: ${{ always() && inputs.mode != 'probe' }}
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            .cache/scored-events.json
            docs/index.html
          if-no-files-found: ignore
